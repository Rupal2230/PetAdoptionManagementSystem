FROM php:8.2-apache

# Install mysqli extension required for MySQL connections
RUN docker-php-ext-install mysqli && docker-php-ext-enable mysqli

# Copy project files to Apache root
COPY . /var/www/html/

# Expose port 80
EXPOSE 80